#import "/rmcs_notebook/template/template.typ": *

== V1 丝杆机构解算与控制

deformable infantry v1 的关节执行器并非直接作用于关节轴，而是通过丝杆机构将电机转角转换为轴向位移，再由连杆机构将位移转换为关节角或轮组相对底盘的位置变化。
因此，v1 的控制问题不是简单的“角度闭环”，而是一个包含几何反解的链式问题：

$
  "motor angle"
  arrow.r
  "screw displacement"
  arrow.r
  "mechanism geometry"
  arrow.r
  "joint angle / wheel pose"
$

=== 建模目标

对 v1，我们希望回答以下问题：

1. 电机角 $theta_m$ 如何映射到丝杆轴向位移 $s$；
2. 丝杆位移 $s$ 如何通过机构几何映射到关节角 $alpha$；
3. 若上层控制给定目标关节角 $alpha^*$，如何反解出目标电机角 $theta_m^*$；
4. 在控制实现中，应当控制丝杆长度、关节角，还是直接控制电机位置。

=== 基本假设

为得到可用而不过度复杂的表达式，作如下假设：

1. 丝杆螺距恒定，忽略弹性形变；
2. 电机与丝杆之间的减速器传动比恒定；
3. 连杆机构为平面机构，铰接点理想无间隙；
4. 不考虑高速冲击时的弹性形变与背隙，仅分析准静态或低速控制区间；
5. 若后续需要加入摩擦、死区和背驱问题，应在本章几何模型之外单独补偿。

=== 符号约定

设：

- 电机侧转角为 $theta_m$；
- 丝杆导程为 $p$，定义丝杆每旋转 $2 pi$ 的轴向位移为 $p$；
- 减速比为 $n$，定义电机转角与丝杆转角满足 $theta_s = theta_m / n$；
- 丝杆轴向位移为 $s$；
- 机构目标关节角为 $alpha$；
- 安装零点对应的丝杆长度为 $s_0$；
- 安装零点对应的电机角为 $theta_(m 0)$。

若存在编码器零点偏置，则将其统一吸收到 $theta_(m 0)$ 中。

=== 电机角到丝杆位移

由丝杆导程定义，丝杆转角 $theta_s$ 对应的轴向位移为：

$
  s - s_0 = frac(p, 2 pi) theta_s
$

又由减速关系：

$
  theta_s = frac(theta_m - theta_(m 0), n)
$

代入得：

$
  s = s_0 + frac(p, 2 pi n) (theta_m - theta_(m 0))
$

这说明从电机角到丝杆位移是一个线性映射，其比例系数为：

$
  k_s = frac(p, 2 pi n)
$

于是可简写为：

$
  s = s_0 + k_s (theta_m - theta_(m 0))
$

=== 丝杆位移到关节角：一般几何形式

接下来考虑连杆机构。对 v1 来说，丝杆位移通常不直接等于关节角，而是通过一组固定长度连杆构成的几何约束间接决定关节角。

为避免把文档写死在某一个具体机构拓扑上，本节先给出一般形式。

设连杆机构中，与丝杆长度直接关联的几何量为 $l(s)$，与关节角直接关联的几何量为 $g(alpha)$。在平面机构中，这两个量由几何约束满足：

$
  F(l(s), g(alpha), c_1, c_2, dots, c_k) = 0
$

其中 $c_1, c_2, dots, c_k$ 表示安装尺寸、固定杆长、偏置量等常数。

若能从约束中解出：

$
  alpha = f(s)
$

则得到正解；
若能从约束中解出：

$
  s = f^(-1)(alpha)
$

则得到反解。

在控制实现上，更推荐构造 $s = f^(-1)(alpha)$，因为上层更自然地给出目标关节角 $alpha^*$，而下层执行器更自然地跟踪电机角 $theta_m^*$。

=== 一个常见简化模型：单三角形机构

若丝杆推动一个平面连杆，且关节角 $alpha$ 与丝杆端点、固定铰点组成三角形，可用余弦定理建立关系。

设三角形三边分别为：

- 固定边 $a$；
- 连杆边 $b$；
- 丝杆等效长度 $l(s)$；

且关节角 $alpha$ 对应于边 $a, b$ 的夹角，则有：

$
  l(s)^2 = a^2 + b^2 - 2 a b cos(alpha)
$

若设丝杆等效长度满足：

$
  l(s) = l_0 + s
$

则可得：

$
  (l_0 + s)^2 = a^2 + b^2 - 2 a b cos(alpha)
$

于是正解为：

$
  alpha = arccos frac(a^2 + b^2 - (l_0 + s)^2, 2 a b)
$

反解为：

$
  s = sqrt(a^2 + b^2 - 2 a b cos(alpha)) - l_0
$

再代回上一节的电机角关系，得到目标电机角：

$
  theta_m^* = theta_(m 0) + frac(2 pi n, p) (s^* - s_0)
$

其中：

$
  s^* = sqrt(a^2 + b^2 - 2 a b cos(alpha^*)) - l_0
$

=== 从目标关节角到目标电机角的完整反解链

综上，若上层给定目标关节角 $alpha^*$，则下层执行器的目标电机角可以按以下链条得到：

$
  alpha^*
  arrow.r^"geometry inverse"
  s^*
  arrow.r^"screw inverse"
  theta_m^*
$

即：

$
  theta_m^* = theta_(m 0) + frac(2 pi n, p) (f^(-1)(alpha^*) - s_0)
$

若将常数项合并，可写成：

$
  theta_m^* = k_theta f^(-1)(alpha^*) + b_theta
$

其中：

$
  k_theta = frac(2 pi n, p)
$

而 $b_theta$ 吸收零点偏置与常数长度偏置。

=== 控制结构分析

v1 的控制至少可分两层：

==== 几何层

几何层负责：

- 根据目标轮距、目标底盘形态或目标关节角，求解目标丝杆长度；
- 根据目标丝杆长度求解目标电机角。

其本质是：

$
  alpha^* arrow.r s^* arrow.r theta_m^*
$

==== 执行层

执行层负责：

- 对电机角 $theta_m$ 做位置闭环；
- 或对丝杆长度误差等效为位置误差后做速度/位置控制；
- 在需要柔顺时加入限幅、死区补偿或力矩限制。

因此，v1 的一个自然实现是：

$
  u = "controller"(theta_m^* - theta_m)
$

其中 $u$ 可以是：

- 电机目标位置；
- 电机目标速度；
- 电机目标力矩。

=== 误差传播分析

与直驱系统相比，v1 的一个突出问题是误差传播链更长。

电机角误差 $delta theta_m$ 会先引起丝杆位移误差：

$
  delta s = frac(p, 2 pi n) delta theta_m
$

再进一步引起关节角误差：

$
  delta alpha = frac(diff alpha, diff s) delta s
$

于是：

$
  delta alpha = frac(diff alpha, diff s) frac(p, 2 pi n) delta theta_m
$

这说明：

1. 丝杆导程越大，电机角误差对机构角误差的放大越明显；
2. 几何映射 $alpha = f(s)$ 在不同工作点的导数不同，会带来非线性增益；
3. 若工作点靠近机构奇异位置，则 $diff alpha / diff s$ 可能急剧变化，控制难度上升。

=== 控制上的工程后果

因此，v1 丝杆方案通常面临如下工程特征：

1. 适合准静态定位，不适合高带宽柔顺控制；
2. 容易受间隙、摩擦、背驱困难影响；
3. 在机构接近奇异位形时，解算和控制增益会显著变化；
4. 若后续要做主动悬挂，不能直接复用 v2 的“关节角近似直接可控”假设。

=== 文档应继续补充的内容

本章当前给出了统一建模框架，但若要形成最终版，还需要补上：

1. v1 实际机构尺寸图；
2. 丝杆导程、减速比、零点定义；
3. 实际使用的几何正反解公式；
4. 丝杆行程极限与关节角极限的对应关系；
5. 当前代码或控制器实现的入口位置。

=== 与 v2 直驱方案的本质区别

最后强调，v1 与 v2 的根本区别不是“电机型号不同”，而是控制链的物理结构不同：

- v1：$theta_m -> s -> alpha$，中间存在显著几何映射；
- v2：$theta_m -> alpha$ 更接近直接映射。

因此，v1 的核心问题首先是几何解算，v2 的核心问题首先是局部关节控制与悬挂控制。
```
